name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test_and_build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Cache Docker layers
      uses: actions/cache@v3
      with:
        path: /tmp/.buildx-cache
        key: ${{ runner.os }}-buildx-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-buildx-

    - name: Build Docker image
      run: |
        docker build -t jota-news:latest .

    - name: Run tests inside container
      run: |
        docker run --rm jota-news:latest python manage.py test

    # Opcional: fazer push para Docker Hub
    #- name: Login to Docker Hub
    #  uses: docker/login-action@v2
    #  with:
    #    username: ${{ secrets.DOCKERHUB_USERNAME }}
    #    password: ${{ secrets.DOCKERHUB_TOKEN }}

    #- name: Push Docker image
    #  run: |
    #    docker tag jota-news:latest yourdockerhubuser/jota-news:latest
    #    docker push yourdockerhubuser/jota-news:latest

    # Opcional: Deploy Lambda via AWS CLI
    #- name: Configure AWS Credentials
    #  uses: aws-actions/configure-aws-credentials@v2
    #  with:
    #    aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
    #    aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    #    aws-region: us-east-1

    #- name: Deploy Lambda Container
    #  run: |
    #    aws lambda update-function-code --function-name your-lambda-function --image-uri youraccount.dkr.ecr.us-east-1.amazonaws.com/jota-news:latest

